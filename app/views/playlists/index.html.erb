<% if logged_in? %>
  <div class="row">
    <aside class="col-md-4">
      <%= render "shared/create_playlist" %>
    </aside>
    <div class="col-md-8 text-center">
      <h3 class="mx-auto text-white font-weight-bold w-50 py-4 pt-5"><%= t ".playlist" %></h3>
      <%= render "shared/list_playlist" %>
    </div>
  </div>
<% end %>
